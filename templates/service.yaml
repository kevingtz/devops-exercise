apiVersion: v1
kind: Service
metadata:
  name: {{ include "td-devopsexercise.fullname" . }}
  labels:
    {{- include "td-devopsexercise.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "td-devopsexercise.selectorLabels" . | nindent 4 }}
